# Use the model in Copilot

Eligible models appear in Copilot Chat as `openlapp/<hashed-id>`. The hashed id is stable and does not contain the provider or model identifier.

Chat, Agent mode, tool calls, streaming, and image input follow the capabilities stored in the local profile. Utility-model settings can be previewed and written only after confirmation.
